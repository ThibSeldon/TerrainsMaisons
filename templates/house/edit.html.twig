{% extends 'base.html.twig' %}

{% block title %}Edit House{% endblock %}

{% block body %}
    <div class="container">
    <h1>Edit House</h1>

    {{ include('house/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('house_index') }}"  class="btn btn-secondary btn-sm" role="button">Retour</a>

    {{ include('house/_delete_form.html.twig') }}

    </div>
{% endblock %}
