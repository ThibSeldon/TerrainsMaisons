<form method="post" action="{{ path('land_allotment_state_delete', {'id': allotment_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allotment_state.id) }}">
    <button class="btn">Delete</button>
</form>
