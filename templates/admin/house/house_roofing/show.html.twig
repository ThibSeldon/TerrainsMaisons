{% extends 'base.html.twig' %}

{% block title %}HouseRoofing{% endblock %}

{% block body %}
    <h1>HouseRoofing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ house_roofing.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ house_roofing.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_house_house_roofing_index') }}">back to list</a>

    <a href="{{ path('admin_house_house_roofing_edit', {'id': house_roofing.id}) }}">edit</a>

    {{ include('admin_house_house_roofing/_delete_form.html.twig') }}
{% endblock %}
