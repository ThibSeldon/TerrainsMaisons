{% extends 'base.html.twig' %}

{% block title %}AllotmentState index{% endblock %}

{% block body %}
    <h1>AllotmentState index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for allotment_state in allotment_states %}
            <tr>
                <td>{{ allotment_state.id }}</td>
                <td>{{ allotment_state.name }}</td>
                <td>
                    <a href="{{ path('land_allotment_state_show', {'id': allotment_state.id}) }}">show</a>
                    <a href="{{ path('land_allotment_state_edit', {'id': allotment_state.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('land_allotment_state_new') }}">Create new</a>
{% endblock %}
