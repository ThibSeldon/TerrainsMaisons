<form method="post" action="{{ path('matching_plot_house_matching_delete', {'id': plot_house_matching.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plot_house_matching.id) }}">
    <button class="btn">Delete</button>
</form>
