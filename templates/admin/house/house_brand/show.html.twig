{% extends 'base.html.twig' %}

{% block title %}HouseBrand{% endblock %}

{% block body %}
    <h1>HouseBrand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ house_brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ house_brand.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ house_brand.url }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ house_brand.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_house_house_brand_index') }}">back to list</a>

    <a href="{{ path('admin_house_house_brand_edit', {'id': house_brand.id}) }}">edit</a>

    {{ include('admin/house/house_brand/_delete_form.html.twig') }}
{% endblock %}
