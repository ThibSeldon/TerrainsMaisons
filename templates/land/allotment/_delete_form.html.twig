<form method="post" action="{{ path('land_allotment_delete', {'id': allotment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allotment.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
